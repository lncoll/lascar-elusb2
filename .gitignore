# Artefactos de compilación Lazarus / FreePascal
*.o
*.ppu
*.or
*.compiled
*.a
*.so

# Binario compilado
/lascar_elusb2

# Estado de sesión del IDE Lazarus
*.lps

# Copias de seguridad locales
/backup/

# Descargas de datos del registrador (generadas por la propia app)
*.csv

# Exportaciones de imagen de la gráfica
*.png

# Script local de subida (no se versiona)
/subir.sh

# Recurso .lrs generado (mecanismo antiguo, ya no se usa)
*.lrs

# Muestras de la app EasyLog de Windows (datos de Luis)
/muestras/
